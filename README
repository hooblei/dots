
dots
====

Installation
------------

    $ git clone git@github.com:hooblei/dots.git ~/.dots
    $ cd ~/.dots
    $ source dots && dotsinit

On first install see [1] how to install/setup vundle


Update
------

    $ cd ~/.dots
    $ git pull
    $ source dots && dotsinit

After install or update run :PluginInstall! to install or upgrade vim plug.


[1] https://github.com/gmarik/Vundle.vim
