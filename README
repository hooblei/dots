
dots
====

Installation
------------

    $ git clone git@github.com:hooblei/dots.git ~/.dots
    $ cd ~/.dots
    $ source dots && dotsinit


Update
------

    $ cd ~/.dots
    $ git pull
    $ source dots && dotsinit

After install or update run :BundeInstall to install or upgrade vim bundles.
